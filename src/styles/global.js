import { createGlobalStyle } from 'styled-components';

export default createGlobalStyle`

html {
  font-size: 24px;
  font-family: "Stratum";
  background: rgba(60, 60, 85);
}

html, body, #app-container {
  height: 100%;
}

`;
